# --- Core ---
Django>=5.2,<5.3
django-environ>=0.11

# --- Admin (Unfold) ---
django-unfold>=0.40
django-import-export>=4.0

# --- i18n ---
django-modeltranslation>=0.19
polib>=1.2          # compile .po -> .mo without GNU gettext (manage.py compilemo)

# --- Auto-translation (admin-triggered uz->ru/en; human-reviewed) ---
deep-translator>=1.11      # free Google backend, no API key
beautifulsoup4>=4.12       # tag-safe HTML (CKEditor) translation

# --- Rich text editor (admin) ---
django-ckeditor-5>=0.2.18

# --- Content / config ---
django-solo>=2.4
Pillow>=10.4
easy-thumbnails>=2.10

# --- CEFR certificate import (PDF -> image + name parse; permissive licences) ---
pypdfium2>=4.30      # PDF render, Apache-2.0/BSD (NOT AGPL like PyMuPDF)
pypdf>=4.3           # PDF text extraction, BSD

# --- Integrations ---
requests>=2.32
user-agents>=2.2.0

# --- Frontend (Tailwind v4 standalone CLI, no Node) ---
django-tailwind-cli>=2.20

# --- Static / serving ---
whitenoise>=6.7
gunicorn>=23.0

# --- Windows timezone data ---
tzdata
